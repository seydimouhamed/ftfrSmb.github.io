import { SecondeToJourPipe } from './seconde-to-jour.pipe';

describe('SecondeToJourPipe', () => {
  it('create an instance', () => {
    const pipe = new SecondeToJourPipe();
    expect(pipe).toBeTruthy();
  });
});
